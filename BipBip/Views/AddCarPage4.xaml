<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BipBip.Views.AddCarPage4"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="#F9FAFB">
    <StackLayout
        Padding="20"
        Spacing="20"
        VerticalOptions="Center">

        <Label
            Margin="0,-20,0,20"
            FontAttributes="Bold"
            FontSize="22"
            HorizontalTextAlignment="Center"
            Text="Ajouter le modèle"
            TextColor="#34495E" />

        <Entry
            x:Name="ModeleVoitureEntry"
            Margin="0,0,0,20"
            BackgroundColor="Transparent"
            FontSize="16"
            HorizontalOptions="FillAndExpand"
            Placeholder="Modèle de véhicule"
            PlaceholderColor="#95A5A6"
            TextColor="#34495E" />

        <!--  Cadre pour le bouton Suivant avec une ombre et un style visuel attractif  -->
        <Frame
            Padding="0"
            BackgroundColor="#3498DB"
            CornerRadius="15"
            HasShadow="True"
            HorizontalOptions="FillAndExpand">
            <Button
                BackgroundColor="Transparent"
                Clicked="OnNextButtonClicked"
                CornerRadius="15"
                FontAttributes="Bold"
                FontSize="18"
                Text="Suivant"
                TextColor="White" />
        </Frame>

    </StackLayout>
</ContentPage>
